package ee.bcs.valiit.sample;

import java.util.Arrays;

public class LoopSample2 {
    public static void main(String[] args) {
        //for(int i = inputArray.length -1; i >= 0; i--){
        //    returnArray[i] = inputArray[inputArray.length -i -1];
        //}
        // length = 5
        //returnArray[i] = inputArray[length -i -1] // i = 4
        //returnArray[i] = inputArray[length -i -1] // i = 3
        //returnArray[i] = inputArray[length -i -1] // i = 2
        //returnArray[i] = inputArray[length -i -1] // i = 1
        //returnArray[i] = inputArray[length -i -1] // i = 0
        /*
        for(int i =0; i < inputArray.length; i++){
            returnArray[i] = inputArray[inputArray.length -i-1]; // i = 0
        }*/
    }
}
